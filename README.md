# Nip

Nip is a compiler for the CatNip programming language.

It's in a very early stage, as of now, it's only an incomplete lexer.


## How to build

To build Nip, you can use cmake.

```
$ cmake -S . -B ./build
$ cmake --build build/
```
